# Technical Test 2 Jakpat by Dicky ardiar

This project was build with React JS

Available Scripts
In the project directory, you can run:

initial install :

npm install
start project :

npm run start
